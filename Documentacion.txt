Sábado 2 Noviembre
Dudas:
+ Celda debería tener como atributo la clase . Hay un caso en si la celda es NA tiene tipo NA puede agregarse
un columna de cualquier tipo de los soportados. Constructores Celda(Class<T> clase) para crear la celda NA y
Celda(Class<T> clase, T valor)
+ Raro que Columna getTipoDato dé todo el nombre completo de la clase mientras que Celda usa getSimpleName
+ Si tenés una columna armada(ejemplo sueldo) no se puede agregar la columna, sólo se puede agregar Filas (???????) 
Se puede implementar un método agregarColumna que reciba una columna ya con celdas???
+ No pude hacer un directorio separado para los tests sin que se pinche el test
+ Hacer el toString de la clase Columna

Las 2 primeras consulto para no tocar y que se rompa lo de Tabla

--------------------
Hicimos:
+ TestColumna
+ TestTabla (faltan testear varios métodos ya implementados)
+ muestreo


Viernes 1 Noviembre
Hicimos:
+ eliminarFila
+ editarCelda
+ volverNACelda
+ visualizar (faltaría alinear de forma bonita)


Jueves 31 octubre

hice funcionar todo hasta q llege a lo de filas jajaja
Exception in thread "main" java.lang.Error: Unresolved compilation problem:
        Type mismatch: cannot convert from List<Celda<T>> to List<Celda>
habria q ver q onda con eso, no llego a enteder q seria una lista de celdas T en una columna
osea si es una lista dentro de la columna deberia ser un tipo de dato especifico y no generico
sino la otra seria usar wildcards pero creo que queda bastante falopa
bueno segun chatgpt lleamos a esto por q no nos dejan usar object jajajaja
osea q si hay q usar wildcards
q seria casi lo mismo q usar object por q es super generico, no entiendo ese requerimiento la vd
bueno voy a pushear cambios, si llegan a querar cambiar algo hagan pull antes
Por ahora lo q funciona es instanciar una tabla y agregar columnas y fijas sin sobrecarga de parametros

Miercoles 30 de octubre
Cree una clase abstracta Etiqueta de la cual se desprenden etiquetaFila y etiquetaColumna. Estas seran gestionadas en la clase Tabla para poder aceptar int, String o default (0 a n) por sobrecarga de metodos. Agregue metodos para agregar filas y columnas, un constructor de tabla, obtener una fila, una columna y una celda.  


Martes 29 de octubre
Estuvimos hablando sobre:
+ Dejamos o no la clase Celda? Definimos que sí, ya que es más facil manejar los valores NA de esta manera 
+ ¿Como vamos a trabajar con los labels? Estos deben ser tanto para las filas como para las columnas. Deben poder ser de tipo numericos, integer o string. En su defecto deben ser numericos de 0 a n. Pensamos en trabajarlos como hash maps <int, L> . No logramos concluir en como implementarlo todavia.
+ Cuando agregamos una fila, esta se va colocando debajo de la ultima fila añadida. Con el Sorting despues el usuario podria decidir ordenador por una o más categorias.
+ Agregamos metodos y atributos a Celda, Columna y Tabla.
+GroupBy. Combinacion.